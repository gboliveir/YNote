import styled from "styled-components";

export const UlStyleComponent = styled.ul`
    position: relative;
    max-width: 1280px;
    margin: 2rem auto 0;
    padding: 0 1rem;
    list-style: none;

    li {
      margin-bottom: 2rem;
    }
`;